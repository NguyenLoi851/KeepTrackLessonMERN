import LoginForm from "../components/auth/LoginForm"
import {useParams} from 'react-router-dom'

const Auth = () => {
    // let {authRoute} = useParams();

    // return (<><LoginForm /></>
        
    // )
    return <div>Hello</div>
}

export default Auth